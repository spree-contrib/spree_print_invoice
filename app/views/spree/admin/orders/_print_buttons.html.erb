<% if @order.completed_at? %>
  <% buttons = "invoice" 
     buttons = buttons.split(",").collect{|b| b.strip } %>
  <%  buttons.each do |button| %>
    <%= button_link_to(t(:invoice_print), spree.admin_order_path(@order, :pdf , :template => button)) %>
  <% end %>
<% end %>  
